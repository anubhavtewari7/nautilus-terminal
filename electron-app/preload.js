// Preload script -- context bridge between renderer and main process.
// Currently empty; add IPC calls here as needed.
window.addEventListener('DOMContentLoaded', () => {
  // Inject app version into the page if needed
  // document.title = 'NAUTILUS Terminal';
});
